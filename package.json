{
  "name": "heladeria-dibiluc",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter backend dev",
    "build": "pnpm --filter backend build",
    "start": "pnpm --filter backend start"
  }
}
